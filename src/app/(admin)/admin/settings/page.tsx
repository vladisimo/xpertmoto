import { SettingsTabBody } from "@/components/admin/settings/settings-tab-body";

/** Default tab — served at /admin/settings. */
export default function SettingsOrganisationPage() {
  return <SettingsTabBody tabKey="organisation" />;
}
