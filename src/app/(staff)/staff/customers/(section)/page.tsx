import { CustomerOverviewTab } from "@/components/staff/customer-overview-tab";

/** Default tab — served at /staff/customers (see the section registry, exact:true). */
export default function CustomersOverviewPage() {
  return <CustomerOverviewTab />;
}
