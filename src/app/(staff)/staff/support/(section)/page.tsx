import { SupportTicketsTab } from "@/components/staff/support-tickets-tab";

/** Default tab — served at /staff/support. */
export default function SupportTicketsPage() {
  return <SupportTicketsTab />;
}
